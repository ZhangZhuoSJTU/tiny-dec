# Post 09: Stack

Stage-9 tests cover:

- stack model invariants and deterministic pretty-printers
- synthetic frame recovery with frame-pointer and stack-pointer-only layouts
- dynamic stack-pointer bailout behavior
- preservation of upstream pending-entry and invalidation state
- fixture-backed contract checks for recovered frame slots and stackless code
- persistent end-to-end harness coverage for `stack:`
